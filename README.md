# sada

R package implementing SADA: Safe and Adaptive Aggregation of Multiple Black-Box Predictions in Semi-Supervised Learning.

## Installation

```r
install.packages("devtools")
devtools::install_github("jw-shan/sada")
```